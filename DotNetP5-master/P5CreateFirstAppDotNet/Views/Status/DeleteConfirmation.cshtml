@model P5CreateFirstAppDotNet.Models.ViewModels.StatusViewModel

@{
    ViewData["Title"] = "Status supprimé";
}

<div class="container text-center mt-5" role="status" aria-live="polite">
    <h2 class="text-muted">@Model.Name</h2>
    <p>a bien été supprimé.</p>
    <a href="@Url.Action("Index", "Status")" class="btn btn-custom">Retour à la liste</a>
</div>
