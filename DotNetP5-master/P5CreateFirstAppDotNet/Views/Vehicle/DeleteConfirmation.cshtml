@model P5CreateFirstAppDotNet.Models.ViewModels.VehicleViewModel
@{
    ViewData["Title"] = "Véhicule supprimé";
}

<div class="container text-center mt-5" role="status" aria-live="polite">
    <h2 class="text-muted">@Model.Year @Model.BrandName @Model.VehicleModelName @Model.TrimName</h2>
    <p>a bien été supprimée.</p>
    <a href="@Url.Action("Index", "Vehicle")" class="btn btn-custom">Retour à l'accueil</a>
</div>

